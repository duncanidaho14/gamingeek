{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        {% for regions in regionsTab %}
            <li>{{regions.nom}} - {{regions.code}}</li>
        {% endfor %}
    </ul>
    <ul>
        {% for regionso in regionsObj %}
            <li>{{regionso.nom}}
                -
            {{regionso.code}}</li>
        {% endfor %}
</ul>

<ul>
    {% for jeuxvideo in jeuxvideorepo %}
        <li>{{jeuxvideo.nom}} -- {{jeuxvideo.prix}} -- {{jeuxvideo.categorie.libelle}} -- {{jeuxvideo.plateforme.nom}} </li>
    {% endfor %}
</ul>
</div>
{% endblock %}
